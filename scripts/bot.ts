import 'dotenv/config';
const token=process.env.TELEGRAM_BOT_TOKEN;const url=process.env.TELEGRAM_WEBAPP_URL;if(!token)throw new Error('Set TELEGRAM_BOT_TOKEN before starting the bot');let offset=0;
async function call(method:string,body:Record<string,unknown>){const r=await fetch(`https://api.telegram.org/bot${token}/${method}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});if(!r.ok)throw new Error(`Telegram ${method} failed: ${r.status}`);return r.json() as Promise<any>}
await call('setMyCommands',{commands:[{command:'start',description:'Открыть пинг таблет'}]});console.log('бот пинг таблет запущен');
while(true){const updates=await call('getUpdates',{offset,timeout:25,allowed_updates:['message']});for(const update of updates.result||[]){offset=update.update_id+1;const chat=update.message?.chat?.id;if(chat&&update.message?.text?.startsWith('/start'))await call('sendMessage',{chat_id:chat,text:'Добро пожаловать в пинг таблет — играй. побеждай. собирай.',reply_markup:{inline_keyboard:[[{text:'ОТКРЫТЬ ПИНГ ТАБЛЕТ',web_app:{url}}]]}});}}
