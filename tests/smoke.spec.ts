import { test, expect } from '@playwright/test';
test('демо-вход и основная навигация',async({page})=>{await page.goto('/');await expect(page.getByText(/Привет, Alex/)).toBeVisible();await expect(page.getByText('всё начинается')).toBeVisible();await page.goto('/leaderboard');await expect(page.getByText('Таблица лидеров.',{exact:true})).toBeVisible();await page.goto('/shop');await expect(page.getByText('Что внутри?')).toBeVisible();});
