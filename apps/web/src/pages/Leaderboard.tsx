import { useState } from 'react';
import { Link } from 'react-router-dom';
import { Trophy, Flame, Layers3, ArrowUpRight } from 'lucide-react';
import { useResource, number, type Row } from '../api';
import { Avatar, PageTitle, League, Skeleton, ErrorView } from '../components/ui';
import { useApp } from '../context';
export default function Leaderboard(){const [metric,setMetric]=useState('rating');const {me,version}=useApp();const {data,error,reload}=useResource<Row[]>(`/leaderboard?metric=${metric}`,version);const value=(p:Row)=>metric==='rating'?p.rating:metric==='streak'?p.streak:p.collection;return <><PageTitle eyebrow="весь клуб" title="Таблица лидеров." description="Дружеское соперничество. Настоящие победы." action={<span className="season-label"><span className="live-dot"/> сезон 01</span>}/><div className="tabs">{[['rating','Рейтинг',Trophy],['streak','Серия побед',Flame],['collectors','Коллекционеры',Layers3]].map(([v,label,Icon]:any)=><button className={metric===v?'active':''} onClick={()=>setMetric(v)} key={v}><Icon size={17}/>{label}</button>)}</div>{error?<ErrorView message={error} retry={reload}/>:!data?<Skeleton/>:<><div className="podium">{[1,0,2].map((idx)=>{const p=data[idx];return p&&<Link className={`podium-card place-${idx+1}`} to={`/players/${p.id}`} key={p.id}><span className="podium-rank">{idx===0?<Trophy size={24}/>:String(idx+1).padStart(2,'0')}</span><Avatar user={p} size="large"/><h3>{p.display_name}</h3><League value={p.league}/><strong>{number(value(p))}</strong><small>{metric==='rating'?'рейтинг Эло':metric==='streak'?'побед подряд':'разных стикеров'}</small></Link>;})}</div><div className="leader-table"><div className="leader-header"><span>МЕСТО</span><span>ИГРОК</span><span className="city-cell">ГОРОД</span><span>{metric==='rating'?'РЕЙТИНГ':metric==='streak'?'СЕРИЯ':'СТИКЕРЫ'}</span><span/></div>{data.map(p=><Link to={`/players/${p.id}`} className={`leader-row ${p.id===me.id?'is-me':''}`} key={p.id}><span className="rank">{String(p.rank).padStart(2,'0')}</span><div className="leader-player"><Avatar user={p}/><span><strong>{p.display_name} {p.id===me.id&&<em>ты</em>}</strong><small>{p.league}</small></span></div><span className="city-cell">{p.city}</span><strong>{number(value(p))}</strong><ArrowUpRight size={16}/></Link>)}</div><p className="footnote">Рейтинг клуба · Учитываются подтверждённые матчи. Стикеры не влияют на результат.</p></>}</>;}
