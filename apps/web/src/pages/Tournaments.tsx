import { useState, useEffect, useRef } from 'react';
import { Link, useParams, useNavigate } from 'react-router-dom';
import { Plus, QrCode, MapPin, CalendarDays, Trophy, Users, ArrowUpRight, Play, Copy, RefreshCw, Camera, X } from 'lucide-react';
import { api, post, useResource, datetime, date, type Row } from '../api';
import { useApp } from '../context';
import { PageTitle, Avatar, Modal, FormError, Skeleton, ErrorView, Empty } from '../components/ui';
import { tg } from '../telegram';
export default function Tournaments(){const {me,version}=useApp();const {data,error,reload}=useResource<Row[]>('/tournaments',version);const [create,setCreate]=useState(false),[scan,setScan]=useState(false),[tab,setTab]=useState('all');const items=data?.filter(t=>tab==='all'?true:tab==='live'?t.status==='LIVE':t.organizer_id===me.id);return <><PageTitle eyebrow="хорошая игра. свои люди." title="Твои турниры." description="Новые соперники. Знакомые столы. Один чемпион." action={<div className="button-row"><button className="btn" onClick={()=>setScan(true)}><QrCode size={18}/>Сканировать QR</button>{['ORGANIZER','ADMIN'].includes(me.role)&&<button className="btn primary" onClick={()=>setCreate(true)}><Plus size={18}/>Создать турнир</button>}</div>}/><div className="tabs">{[['all','Все турниры'],['live','Идут сейчас'],['mine','Мои турниры']].map(([v,l])=><button className={tab===v?'active':''} onClick={()=>setTab(v)} key={v}>{l}</button>)}</div>{error?<ErrorView message={error} retry={reload}/>:!data?<Skeleton/>:!items?.length?<Empty title="Турниров пока нет." text="Скоро здесь появится новая игра."/>:<div className="tournament-grid">{items.map((t,i)=><Link to={`/tournaments/${t.id}`} className={`tournament-tile tile-${i%3}`} key={t.id}><div className="tile-art"><Trophy size={70} strokeWidth={1.25}/><span className={`badge status-${t.status.toLowerCase()}`}>{t.status}</span><span className="tile-number">0{i+1}</span></div><div className="tile-content"><span className="eyebrow">на выбывание</span><h2>{t.title}</h2><p><MapPin size={16}/>{t.location}</p><p><CalendarDays size={16}/>{datetime(t.start_at)}</p><div className="tile-footer"><span><Users size={16}/>{t.player_count} / {t.max_players} игроков</span><ArrowUpRight size={22}/></div></div></Link>)}</div>}{create&&<CreateTournament onClose={()=>setCreate(false)}/ >}{scan&&<ScanJoin onClose={()=>setScan(false)}/>}</>;}
function CreateTournament({onClose}:{onClose:()=>void}){const {refresh}=useApp(),navigate=useNavigate();const [error,setError]=useState(''),[busy,setBusy]=useState(false);return <Modal title="Твой стол. Твой турнир." onClose={()=>!busy&&onClose()}><form className="form-stack" onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);setBusy(true);setError('');try{const r=await post('/tournaments',{title:f.get('title'),description:f.get('description'),location:f.get('location'),max_players:Number(f.get('max_players')),start_at:new Date(String(f.get('start_at'))).toISOString(),registration_deadline:new Date(String(f.get('deadline'))).toISOString()});await refresh();navigate(`/tournaments/${r.id}`);onClose();}catch(err){setError((err as Error).message);}finally{setBusy(false);}}}><label>Название турнира<input name="title" required minLength={3} maxLength={100} placeholder="Воскресный турнир"/></label><label>Место проведения<input name="location" required minLength={2} maxLength={160} placeholder="Клуб и город"/></label><label>Начало<input name="start_at" type="datetime-local" required/></label><label>Регистрация до<input name="deadline" type="datetime-local" required/></label><label>Количество игроков<select name="max_players" defaultValue="8">{[2,4,8,16,32,64].map(n=><option key={n}>{n}</option>)}</select></label><label>Описание<textarea name="description" maxLength={1000} rows={3} placeholder="Место, атмосфера и подробности…"/></label><p className="muted">Турнир на выбывание. Пока регистрация открыта, присоединиться может любой. Награды начисляются после финала.</p><FormError message={error}/><button className="btn primary full" disabled={busy}>{busy?'Создаём…':'Создать турнир'}</button></form></Modal>;}
export function TournamentDetail(){const {id,token}=useParams(),{me,version,refresh,toast}=useApp();const {data:t,error,reload}=useResource<Row>(token?`/invitations/${token}`:`/tournaments/${id}`,version);const [qr,setQr]=useState<Row|null>(null),[result,setResult]=useState<Row|null>(null),[confirmation,setConfirmation]=useState<{kind:string;user?:string}|null>(null),[busy,setBusy]=useState(false),[message,setMessage]=useState('');
 async function action(kind:string,body:unknown={}){if(!t||busy)return;setBusy(true);try{await post(`/tournaments/${t.id}/${kind}`,body);await refresh();toast(kind==='join'?'Ты в игре! Увидимся за столом.':'Турнир обновлён.');setConfirmation(null);}catch(e){toast((e as Error).message);}finally{setBusy(false);}}
 if(error)return <ErrorView message={error} retry={reload}/>;if(!t)return <Skeleton/>;const joined=t.players.some((p:Row)=>p.id===me.id),canJoin=t.status==='OPEN'&&t.registration_deadline>new Date().toISOString(),rounds=[...new Set<number>(t.bracket.map((m:Row)=>m.round))];
 return <><Link to="/tournaments" className="back-link">← Все турниры</Link><PageTitle eyebrow="новая глава твоей игры" title={t.title} description={t.description}/><section className="tournament-detail-banner"><div className="tournament-detail-icon"><Trophy size={65} strokeWidth={1.2}/></div><div><span className={`badge status-${t.status.toLowerCase()}`}>{t.status}</span><p><MapPin size={17}/>{t.location}</p><p><CalendarDays size={17}/>{datetime(t.start_at)}</p><small>Организатор: {t.organizer_name} · Регистрация до {datetime(t.registration_deadline)}</small></div><div className="join-block"><strong>{t.players.length}<span> / {t.max_players}</span></strong><small>ИГРОКИ</small><button className="btn primary" disabled={joined||!canJoin||busy} onClick={()=>action('join',token?{token}:{})}>{joined?'Ты в списке ✓':canJoin?'Участвовать':'Регистрация закрыта'}</button></div></section>
 {t.can_manage&&<div className="organizer-panel"><span className="eyebrow">управление турниром</span><div className="button-row"><button className="btn" onClick={async()=>{try{setQr(await api(`/tournaments/${t.id}/qr`));}catch(e){toast((e as Error).message);}}}><QrCode size={17}/>Поделиться QR</button><button className="btn primary" disabled={!['OPEN','FULL'].includes(t.status)||t.players.length<2||busy} onClick={()=>setConfirmation({kind:'start'})}><Play size={17}/>Начать турнир</button><button className="btn danger" disabled={!['OPEN','FULL','DRAFT'].includes(t.status)||busy} onClick={()=>setConfirmation({kind:'cancel'})}>Отменить турнир</button></div></div>}
 {t.champion_id&&<div className="champion"><Trophy size={34}/><span>чемпион<strong>{t.players.find((p:Row)=>p.id===t.champion_id)?.display_name}</strong></span></div>}
 {rounds.length>0&&<><h2 className="block-title">Путь к победе</h2><div className="bracket" role="region" aria-label="Турнирная сетка" tabIndex={0}>{rounds.map(round=><div className="bracket-round" key={round}><h3>{round===rounds.length?'Финал':round===rounds.length-1?'Полуфинал':round===rounds.length-2?'Четвертьфинал':`Раунд ${round}`}</h3><div className="bracket-matches">{t.bracket.filter((m:Row)=>m.round===round).map((m:Row)=><div className={`bracket-match ${m.status.toLowerCase()}`} key={m.id}><span className="match-id">МАТЧ {m.position+1} · {m.status}</span><div className={m.winner&&m.winner===m.player_a?'winner':''}><span>{m.name_a|| (m.status==='BYE'?'Без соперника':'Определится позже')}</span><strong>{m.score_a??'–'}</strong></div><div className={m.winner&&m.winner===m.player_b?'winner':''}><span>{m.name_b||(m.status==='BYE'?'Без соперника':'Определится позже')}</span><strong>{m.score_b??'–'}</strong></div>{t.can_manage&&m.status==='READY'&&<button className="btn small full" onClick={()=>{setMessage('');setResult(m);}}>Внести счёт</button>}</div>)}</div></div>)}</div></>}
 <h2 className="block-title">Участники <span className="muted">({t.players.length})</span></h2><div className="panel lineup">{t.players.map((p:Row,i:number)=><div className="lineup-row" key={p.id}><span>{String(i+1).padStart(2,'0')}</span><Link to={`/players/${p.id}`}><Avatar user={p}/><strong>{p.display_name}</strong></Link><span>{p.rating} Эло</span>{t.can_manage&&['OPEN','FULL'].includes(t.status)&&<button className="icon-btn" aria-label={`Удалить игрока ${p.display_name}`} onClick={()=>setConfirmation({kind:'remove',user:p.id})}><X size={16}/></button>}</div>)}{!t.players.length&&<Empty title="Будешь первым?" text="Присоединяйся и открой список участников."/>}</div>
 {qr&&<Modal title="Пригласи своих." onClose={()=>setQr(null)}><div className="qr-content"><img src={qr.image} alt="QR-код приглашения на турнир"/><p>Сканируй код, чтобы открыть турнир и участвовать.</p><input aria-label="Ссылка приглашения" readOnly value={qr.url}/><button className="btn primary full" onClick={()=>navigator.clipboard.writeText(qr.url).then(()=>toast('Приглашение скопировано.')).catch(()=>toast('Выделите ссылку выше и скопируйте её.'))}><Copy size={17}/>Скопировать приглашение</button><button className="btn full" onClick={async()=>{try{await post(`/tournaments/${t.id}/qr/regenerate`);setQr(await api(`/tournaments/${t.id}/qr`));toast('Новое приглашение готово. Старый QR-код больше не работает.');}catch(e){toast((e as Error).message);}}}><RefreshCw size={16}/>Обновить QR-код</button></div></Modal>}
 {confirmation&&<Modal title={confirmation.kind==='start'?'Начать турнир?':confirmation.kind==='remove'?'Удалить игрока?':'Отменить турнир?'} onClose={()=>!busy&&setConfirmation(null)}><p>{confirmation.kind==='start'?'После старта список участников закрывается и создаётся сетка. Вносите результаты по завершении матчей.':confirmation.kind==='remove'?'В турнире освободится место.':'Регистрация закроется, а турнир будет отменён.'}</p><button className="btn primary full" disabled={busy} onClick={async()=>{if(confirmation.kind==='remove'){setBusy(true);try{await api(`/tournaments/${t.id}/players/${confirmation.user}`,{method:'DELETE'});await refresh();setConfirmation(null);toast('Игрок удалён.');}catch(e){toast((e as Error).message);}finally{setBusy(false);}}else await action(confirmation.kind);}}>{busy?'Обновляем…':'Подтвердить'}</button></Modal>}
 {result&&<Modal title="Результат матча." onClose={()=>!busy&&setResult(null)}><form onSubmit={async e=>{e.preventDefault();const form=new FormData(e.currentTarget);setBusy(true);setMessage('');try{await post(`/tournaments/${t.id}/matches/${result.id}/result`,{score_a:Number(form.get('a')),score_b:Number(form.get('b'))});await refresh();setResult(null);toast('Счёт записан. Победитель проходит дальше.');}catch(err){setMessage((err as Error).message);}finally{setBusy(false);}}}><p className="muted">Партия до 11 с разницей в 2 очка. Результат окончательный.</p><div className="score-inputs"><label>{result.name_a}<input name="a" aria-label="Счёт первого игрока" type="number" min="0" max="99" required defaultValue="11"/></label><span>:</span><label>{result.name_b}<input name="b" aria-label="Счёт второго игрока" type="number" min="0" max="99" required defaultValue="8"/></label></div><FormError message={message}/><button className="btn primary full" disabled={busy}>{busy?'Записываем…':'Подтвердить счёт'}</button></form></Modal>}</>;
}
function ScanJoin({onClose}:{onClose:()=>void}){const navigate=useNavigate();const [value,setValue]=useState(''),[error,setError]=useState(''),[camera,setCamera]=useState(false);const video=useRef<HTMLVideoElement>(null);const stream=useRef<MediaStream|null>(null);
 function open(raw:string){let token=raw;try{const url=new URL(raw);token=url.pathname.split('/join/')[1]||url.searchParams.get('startapp')||'';}catch{}if(!/^[A-Za-z0-9_-]{32}$/.test(token)){setError('Вставьте приглашение «пинг таблет» или сканируйте QR-код.');return false;}navigate(`/join/${token}`);onClose();return true;}
 useEffect(()=>{if(!camera)return;let active=true;let timer:ReturnType<typeof setTimeout>;const Detector=(window as any).BarcodeDetector;(async()=>{if(!Detector)throw new Error('Сканирование здесь недоступно. Используйте камеру телефона или вставьте приглашение.');const media=await navigator.mediaDevices.getUserMedia({video:{facingMode:'environment'}});if(!active){media.getTracks().forEach(t=>t.stop());return;}stream.current=media;video.current!.srcObject=media;await video.current!.play();const detector=new Detector({formats:['qr_code']});async function scan(){if(!active)return;try{const codes=await detector.detect(video.current);if(codes[0]&&open(codes[0].rawValue))return;}catch{}timer=setTimeout(scan,400);}void scan();})().catch(e=>{setError(e.message);setCamera(false);});return()=>{active=false;clearTimeout(timer);stream.current?.getTracks().forEach(t=>t.stop());};},[camera]);
 return <Modal title="Один скан — и ты в игре." onClose={onClose}><div className="form-stack"><p className="muted">Сканируй QR организатора или вставь приглашение.</p><button className="btn" onClick={()=>{const app=tg();if(app?.initData&&app.showScanQrPopup)app.showScanQrPopup({text:'Сканируйте QR-код турнира'},(text:string)=>open(text));else setCamera(true);}}><Camera size={18}/>Открыть камеру</button>{camera&&<video ref={video} playsInline muted className="qr-video"/>}<label>Ссылка приглашения<input value={value} onChange={e=>setValue(e.target.value)} placeholder="https://…/join/…"/></label><FormError message={error}/><button className="btn primary full" onClick={()=>open(value)}>Открыть турнир <ArrowUpRight size={18}/></button></div></Modal>;
}
