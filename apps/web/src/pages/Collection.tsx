import { useState } from 'react';
import { Link } from 'react-router-dom';
import { ArrowUpRight, LockKeyhole } from 'lucide-react';
import { useResource, type Row } from '../api';
import { useApp } from '../context';
import { PageTitle, StickerCard, Modal, Skeleton, ErrorView } from '../components/ui';
export default function Collection(){const {me,version}=useApp();const {data,error,reload}=useResource<Row[]>('/me/stickers',version);const [rarity,setRarity]=useState('ALL'),[filter,setFilter]=useState('all'),[selected,setSelected]=useState<Row|null>(null);const shown=data?.filter(s=>(filter==='battle'?s.type==='BATTLE'&&s.quantity>0:s.type==='COLLECTION')&&(filter!=='owned'||s.quantity>0)&&(rarity==='ALL'||s.rarity===rarity));return <><PageTitle eyebrow="маленькие находки. большие эмоции." title="Твоя коллекция." description="У каждого стикера своя история. Собери свою." action={<Link className="btn primary" to="/shop">Открыть набор <ArrowUpRight size={18}/></Link>}/><div className="collection-banner"><div><span className="eyebrow">коллекция «первая подача»</span><strong>{me.collection}<span> / 100</span></strong><p>{100-me.collection} стикеров осталось найти.</p></div><div className="collection-ring" style={{'--progress':`${me.collection}%`} as React.CSSProperties}><span>{me.collection}%<small>собрано</small></span></div><img src="/stickers/95.svg" alt=""/></div><div className="filter-row"><div className="tabs">{[['all','Все стикеры'],['owned','Собраны'],['battle','Боевые стикеры']].map(([v,l])=><button className={filter===v?'active':''} key={v} onClick={()=>setFilter(v)}>{l}</button>)}</div><label className="inline-select"><span>Редкость</span><select aria-label="Фильтр редкости" value={rarity} onChange={e=>setRarity(e.target.value)}>{['ALL','COMMON','UNCOMMON','RARE','EPIC','LEGENDARY','MYTHIC'].map(r=><option key={r}>{r}</option>)}</select></label></div>{filter==='battle'&&<p className="info-note">Боевые стикеры переходят победителю. Коллекционные стикеры всегда остаются у тебя.</p>}{error?<ErrorView message={error} retry={reload}/>:!data?<Skeleton/>:<div className="collection-grid">{shown?.map(s=><StickerCard key={s.id} sticker={s} onClick={()=>setSelected(s)}/>)}</div>}{shown?.length===0&&<p className="empty">Пока пусто. Открой набор — и появятся первые находки.</p>}{selected&&<Modal title={selected.name} onClose={()=>setSelected(null)}><div className="sticker-detail"><StickerCard sticker={selected}/><p>{selected.description}</p><p>{selected.quantity?`У тебя ${selected.quantity} ${selected.quantity===1?'экземпляр':'экземпляров'}.`:'Этот стикер ещё предстоит найти.'}</p><Link className="btn primary" to="/shop">Выбрать набор <ArrowUpRight size={18}/></Link></div></Modal>}</>;}
